# MMPI-CHN

国内第一个公开上线的免费 MMPI-2 中文测试（2023 年 2 月上线），纯静态页面，无后端。

**在线使用** → <https://mmpi-chn.github.io/>

---

## 这是什么

- MMPI-2 全部 **567 题**，中英对照，全题中文翻译
- 输出：原始分、T 分、人格剖析图、基础解析
- 计分参照 **中国常模**（2026 年 9 月起，此前为美国常模 + 全局 −8 修正）
- 区分点取 **中国标准 60 T 分**（美国为 65）

## 这不是什么

**这不等同于正版 MMPI-2，不能用于诊断。** 三个具体原因：

1. **译文不同。** 本项目的中文译文是自行翻译的，与正版《MMPI-2 中文简体字版》不同。
   而计分所依据的中国常模是用正版译文建立的 —— 常模能迁移的前提是题目等价，
   这一条我们无法满足，也无法量化偏差。
2. **常模只有位置与离散度。** 中国常模的完整原始分频数分布未公开。我们校正了分布的
   位置与离散度，形状沿用一致性 T 分的统一目标曲线。
3. **常模采样于 1990 年代。** 对标的是 1990 年第四次全国人口普查。它校正的是
   "1990 年代的中美差异"，不校正此后中国人群内部三十多年的变化。

结果可以拿去和专业人士讨论，不要拿它当结论。

---

## 计分是怎么算的

```
答案 → 原始分 → K 校正 → 美国一致性 T 分（查表） → 中国常模 T 分（双参数换算）
```

**中国常模换算**采用位置 + 离散度两参数仿射变换：

```
T_中国 = 50 + (T_美国 − M_T) × 10 / S_T
```

`M_T`、`S_T` 取自 **Cheung, F. M., Song, W. Z., & Zhang, J. X. (1996). The Chinese MMPI-2.
In J. N. Butcher (Ed.), *International Adaptations of the MMPI-2* (pp. 137-161)** 表 6-3 ——
该表给出中国全国常模样本（男 1106 / 女 1108，覆盖七大行政区，对标全国人口统计）
在美国常模尺度上的均值与标准差。**两个参数都是已出版数据，没有估计量。**

覆盖 29 个量表。其余约 110 个量表（Harris-Lingoes 子量表、RC 量表、PSY-5、多数附加量表）
没有已出版的中国常模，页面上会明确标为"无中国常模数据"，不做静默换算。

换算的推导、验证与局限：**[docs/探索纪要-TLDR.md](docs/探索纪要-TLDR.md)**（三分钟）
· **[docs/探索纪要.md](docs/探索纪要.md)**（完整）
· 不熟悉常模/T 分概念的话先看 **[docs/科普-常模与T分.md](docs/科普-常模与T分.md)**

---

## 2026 年 9 月这次修了什么

作者在 2023 年的 README 里写过一句：*"前提是我用的那个哥的代码算一致性 T 分没算错"*。
三年后核对的结果是：**那份美国 T 分查表是真的，但有 9 处手抄错误。**

| 修复 | 影响 |
|---|---|
| 9 处查表数值勘误 | 最严重两处：Sc 男某分段**偏低 37 个 T 分**、Mt 男两格**偏低 60 个 T 分**（把高分读成地板分） |
| Mf 性别下标写死为女性条目 | 潜伏缺陷。两张结果表原本都有 `if(i===7) continue` 跳过 Mf，两张剖析图又各自用对了下标，所以**改动前对用户无可见影响**。现已修正下标，并让 Mf 进入中国常模表 |
| 长短卷开关失效 | `longform` 变量从未被更新，选"短卷"会产生 **197 个未答项**，整张剖析图作废 |
| −8 全局修正 → 逐量表换算 | −8 在各量表上误差达 ±12 分，且 K / Mf(男) / Ma(女) **方向是反的** |
| 修正的有条件触发 | 原逻辑造成不连续：**原始分更高的人可能报出更低的 T 分** |
| T 分未钳位 | 现钳位于 [30, 120] |

旧版本完整保留在 **[`__legacy/`](__legacy/)**，线上可直接访问 <https://mmpi-chn.github.io/__legacy/>，
一个字节都没改。git tag `v1-legacy` 标记了改动前的完整状态。

---

## 开发

```bash
node test/run-node.js      # 形式回归测试（38 项）—— 代码有没有按设计跑
node test/accuracy.js      # 数值准确性检验（13 项）—— 算出来的数对不对
node test/snapshot.js 0 F  # 导出结果页渲染快照（性别 0男/1女，作答 T/F）
```

回归测试的核心验收数据是 Cheung 表 6-3 的 58 格：把中国常模原始分均值喂进查表，
必须复现书上印的 T 分（当前中位数偏差 0.60 T 分，最大 2.0）。这一条同时卡住
查表数据被改坏、K 校正索引错位、性别表选错、勘误被回退。

改动计分层之后**必须**跑一遍测试。

## 待办

数据收集迁出 Formspree（免费额度撞上限会静默丢数据）、localStorage 进度保存
（567 题零存档）、`document.write` 改容器渲染、用手册第七章的中文编码型解析
替掉现有美国来源文本。详见 [docs/探索纪要.md](docs/探索纪要.md) 末节。

---

## 血缘与许可

```
Kevin Timmerman (2008, GPLv3)  →  AstralLing/MMPI2  →  本项目
```

- `origin_js/my_data.js` 的题库与量表定义源自 Kevin Timmerman 2008 年以 GPLv3 发布的
  MMPI-2 实现（见该文件头部原始声明）。其内容的著作权推定属于 University of Minnesota，
  发行权属于 Pearson Clinical Assessment。
- 部分源码取自 [AstralLing/MMPI2](https://github.com/AstralLing/MMPI2)。
  另可参考 [SHENGYUKing/MMPI_Test](https://github.com/SHENGYUKing/MMPI_Test)。
- MMPI®、MMPI®-2 是 University of Minnesota 的注册商标。
- 本项目非营利、无广告，出于教育与公益目的提供。若权利人认为不妥，请开
  [issue](https://github.com/MMPI-CHN/MMPI-CHN.github.io/issues) 告知。

---

*2026 年 9 月的计分层修复、中国常模换算与本仓库文档，由项目作者与
Anthropic Claude Opus 5（`claude-opus-5`，1M 上下文）讨论后共同完成。*
